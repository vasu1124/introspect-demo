apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: introspect-vs
  namespace: default
  labels:
    app.kubernetes.io/instance: introspect
spec:
  hosts:
  - "introspect.vasuhpi.actvirtual.com"
  gateways:
  - istio-system/actvirtual-gw
  http:
  - match:
    - uri:
        regex: /.*
    route:
    - destination:
        port:
          number: 9090
        host: introspect